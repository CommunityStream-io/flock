name: Feathering the Nest

on:
  push:
    branches: [ "*" ] # All branches to include development branches
  # pull_request:
  #   branches: [ "*" ] # All PRs to include forks

jobs:
  lint:
    name: Waterproof feathers (Lint)
    runs-on: ubuntu-latest
    env:
      PACKAGE_TOKEN: ${{ secrets.PACKAGE_TOKEN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24.5.0'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint all projects
        run: npm run lint:all

  test:
    name: Count the flock (Unit Test)
    runs-on: ubuntu-latest
    env:
      PACKAGE_TOKEN: ${{ secrets.PACKAGE_TOKEN }}
      CODECOV_TOKEN: ${{ vars.CODECOV_TOKEN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24.5.0'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build shared library
        run: npx ng build --project=shared

      - name: Run tests with coverage
        run: npm run test:coverage

      - name: Upload unit test coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false

  e2e-matrix:
    name: Calculate E2E Matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Count feature files and generate matrix
        id: set-matrix
        run: |
          # Count feature files
          FEATURE_COUNT=$(find features -name "*.feature" | wc -l)
          echo "Found $FEATURE_COUNT feature files"
          
          # Generate shard array (1 to FEATURE_COUNT)
          SHARDS=$(seq 1 $FEATURE_COUNT | tr '\n' ',' | sed 's/,$//')
          echo "Generated shards: $SHARDS"
          
          # Create matrix JSON
          MATRIX="{\"shard\":[$SHARDS],\"total-shards\":[$FEATURE_COUNT]}"
          echo "Matrix: $MATRIX"
          
          # Set output
          echo "matrix=$MATRIX" >> $GITHUB_OUTPUT

  e2e:
    name: Practice the murmuration (E2E Test)
    runs-on: ubuntu-latest
    needs: e2e-matrix
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.e2e-matrix.outputs.matrix) }}
    env:
      PACKAGE_TOKEN: ${{ secrets.PACKAGE_TOKEN }}
      HEADLESS: true
      CI: true
    container:
      image: ghcr.io/${{ github.repository }}/e2e-test:latest
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Create logs directory
        run: mkdir -p logs/metrics allure-results

      - name: Run E2E tests in container
        run: |
          echo "🚀 Running shard ${{ matrix.shard }}/${{ matrix.total-shards }} in pre-built container..."
          echo 'Starting Angular dev server in background...' &&
          npx ng serve flock-mirage --configuration=test --port=4200 --host=0.0.0.0 &
          SERVER_PID=$! &&
          echo 'Waiting for server to be ready...' &&
          timeout 60 sh -c 'until curl -f http://localhost:4200; do sleep 2; done' &&
          echo 'Server ready! Running tests with pre-installed ChromeDriver...' &&
          npx cross-env CI=true HEADLESS=true BASE_URL=http://localhost:4200 SHARDED_TESTS=true TIMEOUT_TELEMETRY=true wdio run wdio.docker.conf.ts --shard=${{ matrix.shard }}/${{ matrix.total-shards }} &&
          echo 'Tests completed! Stopping server...' &&
          kill $SERVER_PID 2>/dev/null || true

      - name: Upload Allure results as artifact (shard ${{ matrix.shard }})
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-shard-${{ matrix.shard }}-${{ github.run_number }}
          path: allure-results/
          retention-days: 30
        if: always()

      - name: Upload timeout telemetry as artifact (shard ${{ matrix.shard }})
        uses: actions/upload-artifact@v4
        with:
          name: timeout-telemetry-shard-${{ matrix.shard }}-${{ github.run_number }}
          path: logs/metrics/timeout-telemetry-*.json
          retention-days: 30
        if: always()

      - name: Upload test metrics as artifact (shard ${{ matrix.shard }})
        uses: actions/upload-artifact@v4
        with:
          name: test-metrics-shard-${{ matrix.shard }}-${{ github.run_number }}
          path: logs/metrics/test-metrics-*.json
          retention-days: 30
        if: always()

  timeout-analysis:
    name: Analyze Timeout Patterns
    runs-on: ubuntu-latest
    needs: [e2e-matrix, e2e]
    if: always()
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24.5.0'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Download timeout telemetry from all shards
        uses: actions/download-artifact@v4
        with:
          pattern: timeout-telemetry-shard-*-${{ github.run_number }}
          path: logs/metrics/
          merge-multiple: true
        continue-on-error: true

      - name: Download test metrics from all shards
        uses: actions/download-artifact@v4
        with:
          pattern: test-metrics-shard-*-${{ github.run_number }}
          path: logs/metrics/
          merge-multiple: true
        continue-on-error: true

      - name: Analyze timeout telemetry
        run: |
          echo "🔍 TIMEOUT TELEMETRY ANALYSIS"
          echo "=============================="
          echo "📁 Checking for timeout telemetry files..."
          ls -la logs/metrics/timeout-telemetry-*.json 2>/dev/null || echo "No timeout telemetry files found"
          echo ""
          echo "📊 Running CI-specific timeout analysis..."
          npm run analyze:timeouts:ci
        continue-on-error: true

      - name: Analyze test metrics
        run: |
          echo "📊 TEST METRICS ANALYSIS"
          echo "========================"
          echo "📁 Checking for test metrics files..."
          ls -la logs/metrics/test-metrics-*.json 2>/dev/null || echo "No test metrics files found"
          echo ""
          echo "📊 Running metrics analysis..."
          npm run analyze:metrics
        continue-on-error: true

      - name: Generate timeout analysis report
        run: |
          echo "📋 TIMEOUT ANALYSIS SUMMARY"
          echo "==========================="
          echo "Run: ${{ github.run_number }}"
          echo "Commit: ${{ github.sha }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Date: $(date)"
          echo ""
          echo "📁 Artifacts collected:"
          find logs/metrics/ -name "*.json" -type f | wc -l | xargs echo "Total metric files:"
          echo ""
          echo "🔍 For detailed analysis, check the artifacts uploaded by each shard."
        continue-on-error: true

  deploy-allure:
    name: Deploy Allure Report to GitHub Pages
    runs-on: ubuntu-latest
    needs: [e2e-matrix, e2e, timeout-analysis]
    if: always()
    permissions:
      contents: write
      pages: write
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download all Allure results from shards
        uses: actions/download-artifact@v4
        with:
          pattern: allure-results-shard-*-${{ github.run_number }}
          path: allure-results/
          merge-multiple: true
        continue-on-error: true

      - name: Download timeout telemetry from all shards
        uses: actions/download-artifact@v4
        with:
          pattern: timeout-telemetry-shard-*-${{ github.run_number }}
          path: logs/metrics/
          merge-multiple: true
        continue-on-error: true

      - name: Download test metrics from all shards
        uses: actions/download-artifact@v4
        with:
          pattern: test-metrics-shard-*-${{ github.run_number }}
          path: logs/metrics/
          merge-multiple: true
        continue-on-error: true

      - name: Filter out phantom hook failures
        run: node scripts/filter-allure-hooks.js

      - name: Analyze timeout telemetry across all shards
        run: |
          echo "🔍 Analyzing timeout telemetry from all shards..."
          npm run analyze:timeouts || echo "⚠️ Timeout analysis failed, continuing..."
          
          echo "📊 Analyzing test metrics across all shards..."
          npm run analyze:metrics || echo "⚠️ Metrics analysis failed, continuing..."

      - name: Generate Allure Badge
        run: node scripts/generate-allure-badge.js

      - name: Generate Allure Report
        uses: simple-elf/allure-report-action@v1.9
        with:
          allure_results: allure-results
          allure_report: allure-report

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          name: github-pages-${{ github.run_id }}-${{ github.run_attempt }}
          path: allure-report

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
        with:
          artifact_name: github-pages-${{ github.run_id }}-${{ github.run_attempt }}

  build:
    name: Add to the flock (Build)
    runs-on: ubuntu-latest
    env:
      PACKAGE_TOKEN: ${{ secrets.PACKAGE_TOKEN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24.5.0'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install flock dependencies
        run: npm ci

      - name: Build shared library
        run: npx ng build --project=shared

      - name: Build flock-mirage app
        run: npx ng build --project=flock-mirage --configuration=development
